name: VM1 Slow Queue Gate

on:
  pull_request:

permissions:
  contents: read

jobs:
  vm1-slow-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Hold merge queue finalizer
        run: sleep 90

