queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - check-success = vm1-slow-gate
    branch_protection_injection_mode: queue
    autoqueue: true

pull_request_rules: []
